import { StyleSheet } from "react-native";

export const catalogStyle = StyleSheet.create({
    addToCart:{
        marginTop: 5,
        width: 100,
        height: 25,
        backgroundColor: '#fac92f',
        alignItems: 'center',
        borderRadius: 15,
        justifyContent: 'center',
        borderWidth: .1,
    }
})